[Unit]
Description=Dachboard ttyd root shell
After=network.target
StartLimitIntervalSec=0

[Service]
Type=simple
User=root
WorkingDirectory=/root
ExecStart=/usr/local/bin/ttyd -i 127.0.0.1 -p PORT --writable bash
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target

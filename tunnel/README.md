# Tunnel providers

Full doc moved to [docs/PROVIDERS.md](../docs/PROVIDERS.md)
([русская версия](../docs/PROVIDERS.ru.md)).

Short version: `config.yaml → tunnel.provider` is any executable whose
stdout contains the public URL. Scripts live in `tunnel/providers/`.

# Runtime dependencies for the panel daemon.
# Pinned to a known-good floor with an upper bound: an unbounded ">=fastapi"
# silently installs whatever is newest at upgrade time, so `git pull &&
# install.sh` on a production server can pick up a breaking release that was
# never tested against this code.
fastapi>=0.141.1,<1.0
uvicorn>=0.53.0,<1.0
pyyaml>=6.0.3,<7
python-multipart>=0.0.32,<0.1

# Test/lint dependencies live in requirements-dev.txt and are NOT installed by
# deploy/install.sh — a production venv should not carry pytest.
